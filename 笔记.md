## 1)限制运行时间
~~~java
   // 守护线程
    new Thread(() -> {
        try {
            Thread.sleep(TIMEOUT);
            // 超时中断执行
            if (runProcess.isAlive()) runProcess.destroy();
        } catch (InterruptedException e) {
            throw new RuntimeException(e);
        }
    }).start();
~~~
## 2)限制JVM最大内存
~~~java
 //通过JVM的-Xmx命令限制最大执行堆内存
 String runCommand = String.format("java -Xmx256m -Dfile.encoding=UTF-8 -cp %s Main %s", userCodeParentPath, inputArgs);
 // 如果需要更严格的内存限制，要在系统层面去限制，而不是JVM层面的限制
 // Linux系统，可以使用cgroup来实现对某个进程的CPU、内存等资源的分配
~~~
## 3）限制代码-黑白名单
~~~java
 // 程序代码的黑名单
 private static final List<String> BLACK_LIST = Arrays.asList("File", "exec");

 private static final WordTree WORD_TREE = new WordTree();
    
    static {
        // 黑名单初始化
        WORD_TREE.addWords(BLACK_LIST);
    }
~~~
## 4)限制用户的操作权限
 > 限制用户对文件、内存、CPU、网络等资源的操作与访问
 `Java安全管理器（SecurityManager）来实现更严格的限制`
 ~~~java
 public class MySecurityManager extends SecurityManager{

    // 检测用户权限
    @Override
    public void checkPermission(Permission perm) {
        super.checkPermission(perm);
    }

    // 检测程序是否允许读取
    @Override
    public void checkRead(String file) {
        super.checkRead(file);
        throw new RuntimeException("权限异常" + file);
    }

    // 检测程序是否允许写入
    @Override
    public void checkWrite(String file) {
        super.checkWrite(file);
        throw new RuntimeException("权限异常" + file);
    }

    // 检测程序是否允许删除
    @Override
    public void checkDelete(String file) {
        super.checkDelete(file);
        throw new RuntimeException("权限异常" + file);
    }

    // 检测程序是否允许连接网络
    @Override
    public void checkConnect(String host, int port) {
        super.checkConnect(host, port);
        throw new RuntimeException("权限异常" + "host:" +  port);
    }
}

// 设置系统的SecurityManager
String runCommand = String.format("java -Xmx256m -Dfile.encoding=UTF-8 -cp %s;%s -Djava.security.manager=%s Main %s", userCodeParentPath, SECURITY_MANAGER_CLASS_NAME, SECURITY_MANAGER_NAME, inputArgs);
~~~
> 优点：</br>
> 权限控制很灵活，实现简单 </br>
> 缺点：</br>
>      1、粒度太细了，难以精细化控制 </br>
>      2、安全管理器本身也是Java代码，也有可能存在（还是程序上的限制，没到系统的层面）

## 5)运行环境隔离
系统层面上，把用户程序封装到沙箱里，和宿主机（本机/服务器）隔离开
> docker容器技术 （底层是用cgroup、namespace等方式实现的）

使用docker-java

DockerClientConfig:用于定义初始化DockerClient的配置信息
DockerHttpClient: 用于向Docker守护进程（操作Docker的接口）发送请求的客户端，底层封装
DockerClient: 用于操作Docker的客户端，通过DockerClient可以创建、删除、启动、停止容器等操作，最方便的SDK


**远程部署：通过SSH连接到远程服务器，然后使用IDE（如IntelliJ IDEA）进行开发，然后通过SSH连接到远程服务器，然后使用IDE（如IntelliJ IDEA）进行开发，然后通过SSH连接到远程服务器，然后使用IDE（如IntelliJ IDEA）进行开发，然后通过SSH连接到远程服务器，然后使用IDE（如IntelliJ IDEA）进行开发**
</br>


步骤：
- 1、idea tools -> Deployment -> Configuration -> + -> SSH Configuration -> Host:远程服务器ip，Port:22，Username:用户名，Password:密码
- 2、mapping -> Deployment path:本地项目路径，Remote path:远程服务器项目路径
- 3、整个项目 -> 右键 -> Deployment -> Sync Deployed to 服务器 -> 上传项目到远程服务器

如果无法启动程序，修改setting的compiler配置，`-Djdk.lang.Process.launchMechanism=vfork`